# bf-23h38 — Golden references for ensemble, Gaussian, and CONUS DRT=0 fixtures

## Task

Generate golden reference outputs for the ensemble, Gaussian, and CONUS DRT=0
fixtures using the reference decoder (eccodes `grib_dump -j -d` via
`scripts/gen_golden.py`), conforming to the schema in
`docs/golden-json-schema.md` / memory `golden-json-schema`.

## Outcome

All three fixture categories already have complete, schema-conformant golden
references committed under `tests/corpus/golden/` (generated and validated by
sibling beads bf-5lybk, bf-4ncet, bf-3lqm4, bf-552iu, bf-31bq8). This bead's
prior `failure-count:1` was a no-commit/transient failure — the deliverables
themselves are correct. This run re-verified them rather than regenerating
multi-GB files that are already proven correct.

## Verification performed

For each target golden, confirmed: valid JSON, correct `fixture_id`, non-empty
`fields[]`, every field carries all required schema keys, field count equals
the source grib2 message count (eccodes `grib_ls`), and `values.Dense` length
matches the documented grid point count.

| Fixture (category) | Golden file | src msgs | golden fields | values/field | schema |
|---|---|:-:|:-:|:-:|:-:|
| `conus_drt0` (CONUS DRT=0) | conus_drt0.json | 1 | 1 | 104 (13×8) | ✅ |
| `core_gaussian_gdt40` (Gaussian) | core_gaussian_gdt40.json | 104 | 104 | 131072 (512×256) | ✅ |
| `gefs_ensemble_mean_pdt48` (ensemble) | gefs_ensemble_mean_pdt48.json | 71 | 71 | 259920 | ✅ |
| `gefs_member01_pdt41` (ensemble) | gefs_member01_pdt41.json | 71 | 71 | 259920 | ✅ |

Required keys present on every field (0 fields missing keys across all 247
fields total): `center`, `subcenter`, `parameter`, `forecast`, `level`,
`ensemble`, `grid`, `values`, `gdt_template`, `pdt_template`, `drt_template`,
`packing`.

Tooling: eccodes 2.41.0 (`grib_dump`/`grib_ls`), wgrib2 available.

## Acceptance criteria

- [x] Golden reference JSON files exist for ensemble, Gaussian, and CONUS DRT=0
- [x] All outputs match the expected schema
- [x] All files present and git-tracked under `tests/corpus/golden/`

## Re-verification (close run, 2026-07-25)

The prior run committed this notes file but failed before closing the bead
(`failure-count:1`). This run re-verified the deliverables independently rather
than regenerating the multi-GB goldens:

- Loaded every golden with `json.load`: all valid JSON, `fixture_id` matches the
  filename, `fields[]` non-empty, and every field carries all 12 required schema
  keys (`center`, `subcenter`, `parameter`, `forecast`, `level`, `ensemble`,
  `grid`, `values`, `gdt_template`, `pdt_template`, `drt_template`, `packing`).
- Cross-checked the golden field count against the **source grib2 message count**
  via eccodes `grib_ls` footer ("N of N messages"):

  | fixture | src msgs | golden fields | match |
  |---|:-:|:-:|:-:|
  | conus_drt0 | 1 | 1 | ✅ |
  | core_gaussian_gdt40 | 104 | 104 | ✅ |
  | gefs_ensemble_mean_pdt48 | 71 | 71 | ✅ |
  | gefs_member01_pdt41 | 71 | 71 | ✅ |

Source grib2 files present locally under `tests/corpus/{small,large}/`; eccodes
2.41.0 (`grib_dump`/`grib_ls`) and wgrib2 available. No deliverable changes
needed — all acceptance criteria already met by sibling beads.
