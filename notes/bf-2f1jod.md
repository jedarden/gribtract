# Bead bf-2f1jod: Golden JSON fixture for rotated lat/lon

## Summary

The golden JSON fixture for the rotated lat/lon grid (GDT=1) already exists at:
`tests/corpus/golden/rotated_latlon_gdt1_drt0.json`

## Naming Clarification

The bead description referenced `rotated_latlon_gdt0.json`, but this appears to be a typo:
- **GDT=1** = Grid Definition Template for **rotated** lat/lon grids
- **GDT=0** = Grid Definition Template for regular lat/lon grids

The actual fixture name `rotated_latlon_gdt1_drt0.json` correctly reflects:
- GDT=1 (rotated lat/lon grid template)
- DRT=0 (simple packing)

## Fixture Creation

The fixture was created automatically by the generator script `scripts/gen_grib2_rotated.py` when bead bf-1ea3dw was completed. The script generates both:
1. The GRIB2 file: `tests/corpus/small/rotated_latlon_gdt1_drt0.grib2`
2. The golden JSON: `tests/corpus/golden/rotated_latlon_gdt1_drt0.json`

## Acceptance Criteria Status

✅ Golden JSON exists (with correct GDT=1 naming)
✅ JSON matches the decoded GRIB2 file structure (generated by same script)
✅ JSON follows the same schema as other golden fixtures (verified structure)

## Fixture Details

- **Grid**: 3x3 rotated lat/lon, south pole at (30°N, 0°E), angle=0°
- **Packing**: R=270.0, E=0, D=0, N=8 (simple packing)
- **Data**: 9 values [270.0 .. 278.0] K (temperature)
- **Fixture ID**: `rotated_latlon_gdt1_drt0`
